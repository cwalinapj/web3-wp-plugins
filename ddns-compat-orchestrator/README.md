# ddns-compat-orchestrator

## What it does
WordPress plugin for ddns compat orchestrator.

## Install
1. Copy this folder to `wp-content/plugins/ddns-compat-orchestrator`
2. Activate in WordPress admin → Plugins.

## Configure
- Check plugin settings under WordPress admin.

## Development
Use the `web3-wp-plugins` docker stack for local testing.
